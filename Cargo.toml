[package]
name = "proc-dash"
version = "0.1.0"
edition = "2021"

[dependencies]
ratatui = "0.29"
crossterm = "0.28"
nix = { version = "0.29", features = ["signal", "process", "user"] }
libc = "0.2"
unicode-width = "0.2"

[profile.release]
opt-level = 3
lto = true
